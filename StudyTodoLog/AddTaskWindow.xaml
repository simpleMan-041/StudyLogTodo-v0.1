<Window x:Class="StudyTodoLog.AddTaskWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:StudyTodoLog"
        mc:Ignorable="d"
        Title="タスク追加" Height="380" Width="600"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="タスク名(必須)" 
                   Style="{StaticResource FormLabelTextStyle}"/>
        
        <TextBox x:Name="TitleTextBox" 
                 Grid.Row="1" 
                 Style="{StaticResource InputTitleTextBoxStyle}"/>

        <TextBlock Grid.Row="2" 
                   Text="メモ(任意)"
                   Style="{StaticResource FormLabelTextStyle}"/>
        
        <TextBox x:Name="MemoTextBox" 
                 Grid.Row="3"
                 Style="{StaticResource InputMemoTextBoxStyle}"/>
        
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <!--保存ボタンとの間隔を置くためのMargin-->
            <Button Content="キャンセル"
                    Style="{StaticResource FormActionButtonStyle}"
                    Margin="0,0,12,0" 
                    Click="Cancel_Click"/>
            <Button Content="保存"
                    Style="{StaticResource FormActionButtonStyle}"
                    Click="Save_Click"/>
        </StackPanel>
    </Grid>
</Window>
